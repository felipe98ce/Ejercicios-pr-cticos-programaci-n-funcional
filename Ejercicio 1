from functools import reduce

def descom(valor):
    return valor % 10

def sumador(acumulador=0, elemento=0):
    return acumulador + elemento

print(reduce(sumador,list(map(descom,[13,25,27,29,41,85,1029]))))
